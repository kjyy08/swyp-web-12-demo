# SWYP Web 12기 데모데이 방명록 - 디자인 가이드

> 스위프 웹 12기 데모데이 행사 방명록 앱의 UI/UX 디자인 명세 문서입니다.
> frontend-dev가 구현 시 이 문서를 참조합니다.

---

## 디자인 방향

- **톤앤매너**: 밝고 친근하며 모던한 분위기. 행사의 활기찬 에너지를 반영.
- **레이아웃**: 카드형 레이아웃, 적절한 그림자와 라운딩으로 입체감 부여
- **반응형**: 모바일 우선 설계 (Mobile First)
- **접근성**: 명확한 색상 대비, 읽기 쉬운 폰트 크기

---

## 1. 색상 팔레트

### Primary (주 색상)
행사의 활기차고 열정적인 분위기를 표현하는 따뜻한 인디고/바이올렛 계열.

| 이름 | HEX | Tailwind 클래스 | 용도 |
|------|-----|----------------|------|
| Primary | `#6366F1` | `bg-indigo-500` | 주요 버튼, 강조 요소 |
| Primary Dark | `#4F46E5` | `bg-indigo-600` | 버튼 hover 상태 |
| Primary Light | `#E0E7FF` | `bg-indigo-100` | 배경 포인트, 뱃지 배경 |

### Secondary (보조 색상)
| 이름 | HEX | Tailwind 클래스 | 용도 |
|------|-----|----------------|------|
| Secondary | `#F59E0B` | `bg-amber-400` | 액센트, 강조 텍스트 |
| Secondary Dark | `#D97706` | `bg-amber-500` | hover 상태 |

### Background / Surface
| 이름 | HEX | Tailwind 클래스 | 용도 |
|------|-----|----------------|------|
| Background | `#F8FAFC` | `bg-slate-50` | 전체 페이지 배경 |
| Surface | `#FFFFFF` | `bg-white` | 카드, 폼 배경 |
| Surface Alt | `#F1F5F9` | `bg-slate-100` | 입력 필드 배경 |
| Border | `#E2E8F0` | `border-slate-200` | 카드 테두리, 구분선 |

### Text (텍스트)
| 이름 | HEX | Tailwind 클래스 | 용도 |
|------|-----|----------------|------|
| Text Primary | `#0F172A` | `text-slate-900` | 주요 텍스트 |
| Text Secondary | `#475569` | `text-slate-600` | 보조 텍스트 |
| Text Muted | `#94A3B8` | `text-slate-400` | 비활성, 힌트 텍스트 |
| Text On Primary | `#FFFFFF` | `text-white` | 주요 버튼 위 텍스트 |

### 직군별 뱃지 색상

각 직군을 시각적으로 구분하는 색상 체계.

| 직군 | 배경색 HEX | 텍스트색 HEX | Tailwind 클래스 조합 |
|------|-----------|------------|---------------------|
| PM | `#EDE9FE` | `#6D28D9` | `bg-violet-100 text-violet-700` |
| Designer | `#FCE7F3` | `#BE185D` | `bg-pink-100 text-pink-700` |
| Developer | `#DBEAFE` | `#1D4ED8` | `bg-blue-100 text-blue-700` |
| Other | `#F1F5F9` | `#475569` | `bg-slate-100 text-slate-600` |

---

## 2. 타이포그래피

### 폰트 패밀리

```css
/* Google Fonts 사용 권장 */
/* index.html <head>에 추가 */
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;600;700&display=swap');
```

- **기본 폰트**: `Noto Sans KR` (한국어 최적화, 행사 분위기에 맞는 가독성)
- **대체 폰트**: `system-ui, -apple-system, sans-serif`

```css
/* globals.css 또는 index.css */
body {
  font-family: 'Noto Sans KR', system-ui, -apple-system, sans-serif;
}
```

### 타이포그래피 스케일

| 역할 | 크기 | 굵기 | Line Height | Tailwind 클래스 |
|------|------|------|------------|----------------|
| 페이지 제목 (h1) | 32px / 2rem | 700 Bold | 1.2 | `text-4xl font-bold` |
| 섹션 제목 (h2) | 24px / 1.5rem | 600 SemiBold | 1.3 | `text-2xl font-semibold` |
| 카드 제목 (h3) | 18px / 1.125rem | 600 SemiBold | 1.4 | `text-lg font-semibold` |
| 부제목 | 16px / 1rem | 500 Medium | 1.5 | `text-base font-medium` |
| 본문 | 14px / 0.875rem | 400 Regular | 1.6 | `text-sm` |
| 캡션 / 메타 | 12px / 0.75rem | 400 Regular | 1.5 | `text-xs` |
| 버튼 | 14px / 0.875rem | 600 SemiBold | 1 | `text-sm font-semibold` |

---

## 3. 컴포넌트별 레이아웃 명세

### 3.1 Header 컴포넌트

**목적**: 앱의 정체성을 명확히 하고 행사 분위기를 전달하는 상단 영역.

```
┌─────────────────────────────────────────────┐
│  🎉  SWYP Web 12기 데모데이 방명록          │  ← h1, text-4xl font-bold
│      당신의 이야기를 남겨주세요             │  ← 부제, text-slate-500
└─────────────────────────────────────────────┘
```

**스펙:**
- 배경: `bg-white` + 하단 그림자 `shadow-sm border-b border-slate-200`
- 내부 컨테이너: `max-w-5xl mx-auto px-4 py-8 text-center`
- 제목: `text-3xl md:text-4xl font-bold text-slate-900`
- 부제: `mt-2 text-base text-slate-500`
- 상단 이모지/아이콘: 시각적 흥미 요소 추가 (선택)

**Tailwind 예시:**
```jsx
<header className="bg-white border-b border-slate-200 shadow-sm">
  <div className="max-w-5xl mx-auto px-4 py-10 text-center">
    <h1 className="text-3xl md:text-4xl font-bold text-slate-900">
      🎉 SWYP Web 12기 데모데이 방명록
    </h1>
    <p className="mt-3 text-base text-slate-500">
      당신의 이야기를 남겨주세요
    </p>
  </div>
</header>
```

---

### 3.2 GuestbookForm 컴포넌트

**목적**: 닉네임, 직군, 메시지를 입력하는 카드형 폼.

```
┌─────────────────────────────────────────────────────┐
│  방명록 남기기                                       │  ← 섹션 제목
│                                                     │
│  닉네임                                             │
│  ┌─────────────────────────────────────────────┐   │
│  │ 닉네임을 입력하세요 (최대 20자)             │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  직군                                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────┐ │
│  │   PM     │ │Designer  │ │Developer │ │ Other │ │
│  └──────────┘ └──────────┘ └──────────┘ └───────┘ │
│                                                     │
│  메시지                                             │
│  ┌─────────────────────────────────────────────┐   │
│  │ 오늘 행사의 소감이나 응원 메시지를          │   │
│  │ 남겨주세요 (최대 200자)          [15/200]   │   │
│  └─────────────────────────────────────────────┘   │
│                                              ┌────┐ │
│                                              │ 등록│ │
│                                              └────┘ │
└─────────────────────────────────────────────────────┘
```

**스펙:**
- 카드: `bg-white rounded-2xl shadow-md border border-slate-200 p-6`
- 폼 제목: `text-lg font-semibold text-slate-900 mb-4`
- 레이블: `block text-sm font-medium text-slate-700 mb-1`

**입력 필드 (Input):**
- 기본: `w-full px-4 py-2.5 rounded-xl border border-slate-200 bg-slate-50 text-sm text-slate-900 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-transparent transition`

**직군 선택 (Radio Button → 버튼 스타일):**
- 미선택: `px-4 py-2 rounded-lg text-sm font-medium border border-slate-200 text-slate-600 bg-white hover:bg-slate-50 transition cursor-pointer`
- 선택됨: 직군별 색상 적용 (예: PM은 `bg-violet-100 text-violet-700 border-violet-300`)

**텍스트 영역 (Textarea):**
- `w-full px-4 py-3 rounded-xl border border-slate-200 bg-slate-50 text-sm text-slate-900 placeholder:text-slate-400 resize-none focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-transparent transition`
- 높이: `rows={4}` (약 96px)
- 글자 수 카운터: 우측 하단, `text-xs text-slate-400`

**등록 버튼:**
- `px-6 py-2.5 bg-indigo-500 hover:bg-indigo-600 text-white text-sm font-semibold rounded-xl transition shadow-sm`
- 로딩 상태: 비활성 + 스피너 표시

---

### 3.3 GuestbookCard 컴포넌트

**목적**: 개별 방명록 항목을 카드 형태로 표시.

```
┌───────────────────────────────────────────┐
│  홍길동  [Developer]               [삭제] │  ← 닉네임 + 뱃지 + 삭제 버튼
│                                           │
│  오늘 데모데이 정말 재밌었어요!           │  ← 메시지 본문
│  열심히 준비한 팀들 모두 고생했습니다!   │
│                                           │
│  2026.02.21 오후 3:45                    │  ← 등록 시간 (우측 정렬)
└───────────────────────────────────────────┘
```

**스펙:**
- 카드: `bg-white rounded-2xl shadow-sm border border-slate-200 p-5 hover:shadow-md transition-shadow`
- 헤더 영역: `flex items-center justify-between mb-3`
- 닉네임: `text-base font-semibold text-slate-900`
- 뱃지: `ml-2 px-2.5 py-0.5 rounded-full text-xs font-medium` + 직군별 색상
- 삭제 버튼: `p-1.5 rounded-lg text-slate-400 hover:text-red-500 hover:bg-red-50 transition`
- 메시지: `text-sm text-slate-700 leading-relaxed mb-4`
- 타임스탬프: `text-xs text-slate-400 text-right`

**시간 포맷**: `YYYY.MM.DD 오전/오후 H:MM` 형식

**Tailwind 예시:**
```jsx
<article className="bg-white rounded-2xl shadow-sm border border-slate-200 p-5 hover:shadow-md transition-shadow">
  <div className="flex items-center justify-between mb-3">
    <div className="flex items-center gap-2">
      <span className="text-base font-semibold text-slate-900">홍길동</span>
      <span className="px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-700">
        Developer
      </span>
    </div>
    <button className="p-1.5 rounded-lg text-slate-400 hover:text-red-500 hover:bg-red-50 transition">
      {/* 삭제 아이콘 */}
    </button>
  </div>
  <p className="text-sm text-slate-700 leading-relaxed mb-4">
    메시지 내용
  </p>
  <time className="block text-xs text-slate-400 text-right">
    2026.02.21 오후 3:45
  </time>
</article>
```

---

### 3.4 GuestbookList 컴포넌트

**목적**: 방명록 카드 목록을 반응형 그리드로 표시.

**스펙:**
- 컨테이너: `max-w-5xl mx-auto px-4 py-8`
- 섹션 제목 영역: `flex items-center justify-between mb-6`
  - 제목: `text-xl font-semibold text-slate-900`
  - 카운트: `text-sm text-slate-500` (예: "총 42개")
- 그리드: 반응형 (아래 브레이크포인트 참조)
- 빈 상태: 중앙 정렬, 안내 메시지 + 아이콘

**빈 상태 (Empty State):**
```jsx
<div className="text-center py-16 text-slate-400">
  <p className="text-4xl mb-3">📝</p>
  <p className="text-sm">아직 방명록이 없어요. 첫 번째로 남겨보세요!</p>
</div>
```

---

## 4. 반응형 브레이크포인트

Tailwind CSS 기본 브레이크포인트 사용.

| 구간 | 범위 | 그리드 열 수 | Tailwind 클래스 |
|------|------|------------|----------------|
| 모바일 | `< 640px` | 1열 | `grid-cols-1` |
| 태블릿 | `640px ~ 1024px` | 2열 | `sm:grid-cols-2` |
| 데스크탑 | `> 1024px` | 3열 | `lg:grid-cols-3` |

**그리드 Tailwind 예시:**
```jsx
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
  {/* GuestbookCard 컴포넌트들 */}
</div>
```

---

## 5. 간격/여백 시스템

Tailwind CSS의 4px 기반 스페이싱 스케일을 따릅니다.

| 단위 | px | 용도 |
|------|-----|------|
| `gap-2` | 8px | 뱃지-닉네임 간격, 인라인 요소 |
| `gap-4` | 16px | 카드 그리드 간격, 폼 필드 간격 |
| `gap-6` | 24px | 섹션 내부 요소 간격 |
| `p-4` | 16px | 작은 카드 내부 여백 |
| `p-5` | 20px | 카드 내부 여백 (기본) |
| `p-6` | 24px | 폼 카드 내부 여백 |
| `px-4` | 16px | 모바일 좌우 여백 |
| `py-8` | 32px | 섹션 상하 여백 |
| `py-10` | 40px | 헤더 상하 여백 |
| `mb-3` | 12px | 카드 헤더 하단 마진 |
| `mb-4` | 16px | 메시지 하단 마진 |
| `mb-6` | 24px | 섹션 제목 하단 마진 |

---

## 6. 전체 페이지 레이아웃

```
┌─────────────────────────────────────────────────────┐
│                      Header                          │  bg-white, shadow-sm
├─────────────────────────────────────────────────────┤
│              max-w-5xl mx-auto px-4                  │
│                                                     │
│  ┌───────────────────────────────────────────────┐  │
│  │              GuestbookForm                    │  │  bg-white card
│  └───────────────────────────────────────────────┘  │
│                                                     │
│  방명록 목록 (n개)                                  │  섹션 타이틀
│                                                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐              │
│  │  Card   │ │  Card   │ │  Card   │              │  3열 그리드 (desktop)
│  └─────────┘ └─────────┘ └─────────┘              │
│  ┌─────────┐ ┌─────────┐                           │
│  │  Card   │ │  Card   │                           │
│  └─────────┘ └─────────┘                           │
│                                                     │
└─────────────────────────────────────────────────────┘
  bg-slate-50 (전체 배경)
```

**페이지 루트:**
```jsx
<div className="min-h-screen bg-slate-50">
  <Header />
  <main className="max-w-5xl mx-auto px-4 py-8 space-y-8">
    <GuestbookForm />
    <GuestbookList />
  </main>
</div>
```

---

## 7. 애니메이션 / 인터랙션

| 요소 | 효과 | Tailwind 클래스 |
|------|------|----------------|
| 버튼 | hover 색상 전환 | `transition` (150ms) |
| 카드 | hover 그림자 강화 | `hover:shadow-md transition-shadow` |
| 입력 필드 | focus ring 표시 | `focus:ring-2 focus:ring-indigo-400 transition` |
| 직군 버튼 | 선택 상태 색상 전환 | `transition` |
| 새 카드 등록 | 목록 상단에 자연스럽게 추가 | 리스트 key 기반 React 렌더링 |

---

## 8. 아이콘 가이드

`lucide-react` 사용 권장 (React 환경과 Tailwind와의 호환성 우수).

| 용도 | 아이콘 | 컴포넌트 |
|------|--------|----------|
| 삭제 버튼 | 휴지통 | `<Trash2 size={16} />` |
| 방명록 없음 | 노트 | `<FileText size={40} />` |
| 등록 버튼 로딩 | 스피너 | `<Loader2 size={16} className="animate-spin" />` |

**설치:**
```bash
npm install lucide-react
```

---

## 9. 데이터 모델 매핑

백엔드 API 응답 기준 UI 렌더링 매핑.

| 필드 | 타입 | UI 표시 위치 | 비고 |
|------|------|------------|------|
| `id` | int | (내부 key, 삭제 요청 시 사용) | 미표시 |
| `nickname` | string | 카드 헤더 닉네임 | 최대 20자 |
| `role` | string | 카드 뱃지 | `PM \| Designer \| Developer \| Other` |
| `message` | string | 카드 본문 | 최대 200자, `leading-relaxed` |
| `created_at` | datetime | 카드 하단 타임스탬프 | ISO 8601 → 로컬 시간 포맷 변환 |

**시간 포맷 변환 예시 (TypeScript):**
```ts
const formatDate = (isoString: string): string => {
  const date = new Date(isoString);
  return date.toLocaleString('ko-KR', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
    hour: 'numeric',
    minute: '2-digit',
    hour12: true,
  });
};
```

---

*문서 작성: ui-designer | 최종 업데이트: 2026-02-21*
